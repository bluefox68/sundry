# 概念：
1.高阶函数:
2.带函数值的函数:
3.自应用函数:
4.自复制函数:

# 解决的问题：
1.函数作为返回值返回时，高阶函数执行完变量从栈中移除与返回的函数执行时如何寻找自由变量的问题。
2.函数作为参数传递时，自由变量的取值问题。

# 基于栈的实现与基于“堆”的实现的区别：

# 执行上下文对象
1.全局变量对象(VO)；
2.活动变量对象(AO)。

# 执行上下文对象的属性一般有:
1.声明的变量：
2.声明的函数：
3.函数的形参：
# 上下文代码的处理阶段(分为2个)：
1.进入执行上下文:进入执行上下文时，即对声明的变量进行初始化，代码并没有执行。且函数的声明在变量的声明之前。

2.执行代码:

# 



